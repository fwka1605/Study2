task loadfile <<{
  def files = file('C:/Users/NVLCLT13/Documents/nouvelle/Git/server/Study/Gradle/project').listFiles.sort()
  files.each{
    File file ->
      if(File.isFile()){
        ant.loadfile(srcFile: file,property:file.name)
        println "*** $file.name ***"
        println "${ant.properties[file.name]}"
      }
  }
}
